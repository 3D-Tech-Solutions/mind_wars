# Game Asset Structure

This directory is reserved for game-specific branded assets.

Suggested rollout structure:

- `memory/`
- `logic/`
- `attention/`
- `spatial/`
- `language/`

Add subfolders as assets are introduced so the Flutter bundle reflects the category and game taxonomy from [docs/branding.md](../../docs/branding.md).