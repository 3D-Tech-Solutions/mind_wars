# Brand Fonts

Place the approved brand fonts here before enabling them in [pubspec.yaml](../../pubspec.yaml).

Planned families:

- `Orbitron`
- `Space Mono`

Do not register these fonts in Flutter until the actual font files are committed.